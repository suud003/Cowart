interface:
  display_name: "Yogurt AI Auto Compose"
  short_description: "Route one brief into visual and editable canvas outputs"
  brand_color: "#2F63D7"
  default_prompt: "Use $cowart-auto-compose to route my mixed requirement, create a near-final full-page visual preview, and rebuild its planned regions as images, native editable diagrams, and source-linked evidence cards."

policy:
  allow_implicit_invocation: true
