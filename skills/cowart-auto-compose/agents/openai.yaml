interface:
  display_name: "Yogurt AI Auto Compose"
  short_description: "Route one brief into visual and editable canvas outputs"
  brand_color: "#2F63D7"
  default_prompt: "Use $cowart-auto-compose to route my mixed requirement, build a structured page layout plan, and create one reviewable full-canvas layout blueprint before generating images, native editable diagrams, and evidence cards into its approved slots."

policy:
  allow_implicit_invocation: true
