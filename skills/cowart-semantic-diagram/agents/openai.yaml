interface:
  display_name: "Yogurt Semantic Diagram"
  short_description: "把需求绘成官方 Excalidraw 原生可编辑图"
  brand_color: "#6965DB"
  default_prompt: "Use $cowart-semantic-diagram to turn my selected Yogurt AI notes into a diagram made only from native Excalidraw rectangles with bound text, frames, and bound arrows on the current canvas."

policy:
  allow_implicit_invocation: true
