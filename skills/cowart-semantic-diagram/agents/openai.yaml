interface:
  display_name: "Yogurt Semantic Diagram"
  short_description: "把需求绘成 Excalidraw 风格的原生可编辑图"
  brand_color: "#FF6234"
  default_prompt: "Use $cowart-semantic-diagram to turn my selected Yogurt AI notes into an Excalidraw-style diagram made only from native editable cards, semantic zones, text, and bound relations on the current canvas."

policy:
  allow_implicit_invocation: true
