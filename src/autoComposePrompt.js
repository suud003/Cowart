export const AUTO_COMPOSE_SKILL_NAME = '$cowart-auto-compose'

export const AUTO_COMPOSE_QUICK_PROMPT = [
  `使用 ${AUTO_COMPOSE_SKILL_NAME} 处理我的混合需求。`,
  '先识别哪些内容需要视觉图片、哪些需要原生可编辑框线图、哪些应保留为证据与约束卡片。',
  '先建立结构化页面布局计划，再生成一张低文字的整页画布布局蓝图并放到当前画布；蓝图必须展示页面边界、各内容分区、相对尺寸、层级、留白与阅读顺序，不能生成单张概念图、场景图或海报来代替。',
  '展示分流和分区预览后停下来等我确认。确认后，按已批准的布局槽位分别生成图片、原生可编辑框线图和证据卡片；产品语义必须来自原始需求与可追溯材料，不能从蓝图像素推断，也不能栅格化。',
  '除非我明确要求，否则不要创建 PRD。'
].join('\n')

export const AUTO_COMPOSE_ROUTING_HINT = [
  '任务路由：如果请求同时包含视觉场景、流程/关系结构、事实/约束中的至少两类，优先使用 $cowart-auto-compose。',
  '先从结构化计划生成一张低文字的整页画布布局蓝图，展示图片区、原生可编辑结构区和证据区的页面位置、比例、层级与阅读顺序；待用户确认后，再按对应槽位生成并落位各部分。',
  '布局蓝图不是概念图，也不是语义来源；语义必须来自用户需求和可追溯材料，执行时不能从蓝图像素或 OCR 反推。单一明确任务继续使用最匹配的现有能力。'
].join('\n')
