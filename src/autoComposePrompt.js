export const AUTO_COMPOSE_SKILL_NAME = '$cowart-auto-compose'

export const AUTO_COMPOSE_QUICK_PROMPT = [
  `使用 ${AUTO_COMPOSE_SKILL_NAME} 处理我的混合需求。`,
  '先识别哪些内容需要视觉图片、哪些需要原生可编辑框线图、哪些应保留为证据与约束卡片。',
  '先建立结构化页面计划和每个槽位的内容规格，再生成一张接近成品的整页视觉预演并放到当前画布；预演必须把场景图、流程图、卡片、标题层级、留白与阅读顺序一起粗略渲染出来，不能只画空白占位框、抽象框线蓝图，也不能用单张概念图或海报代替整页。',
  '随后严格按同一组槽位分别生成视觉图片，并把预演中的流程与关系重建为原生可编辑对象，把事实与约束重建为可追溯卡片；语义必须来自原始需求与材料，不能从预演图片或 OCR 猜测，也不能把结构栅格化。',
  '整页预演最多尝试两次；若图像服务返回鉴权、访问、限流、网络或服务错误，停止重复生成，只把视觉槽标记为待重试，并立即按已验证页面计划继续生成 html-line-svg 原生可编辑线框图和证据卡。不要让预演失败阻塞原生结构。',
  '遵循 Agent 面板当前的执行方式：自动执行时连续完成整页预演和分区落位，不额外等待布局确认；分步确认时在整页预演后暂停一次。',
  '除非我明确要求，否则不要创建 PRD。'
].join('\n')

export const AUTO_COMPOSE_ROUTING_HINT = [
  '任务路由：如果请求同时包含视觉场景、流程/关系结构、事实/约束中的至少两类，优先使用 $cowart-auto-compose。',
  '先从结构化计划生成一张接近成品的整页视觉预演：各图片区要有代表性画面，流程区要有可辨认的节点与连线，证据区要有真实层级和内容密度；不要只生成空白框线布局。',
  '再把预演按原槽位拆分实现：图片使用对应预演区域作为视觉参考，流程与关系依据原始材料重建为原生可编辑对象，证据与约束重建为可追溯卡片。结构化计划控制最终坐标和防碰撞，预演图片只控制视觉与构图，不能作为语义来源。单一明确任务继续使用最匹配的现有能力。',
  '预演图像生成失败时最多重试一次；随后仅降级视觉槽，继续按结构化计划落地 html-line-svg 原生图和证据卡。自动执行模式不得因此发起确认或把整个任务判失败。'
].join('\n')
